#include <stdio.h>

/**
 * main - this adds two intergers
 * Return: Always 0 success
 */
int main(void)
{
	int x = 20;
	int y = 10;
	int result;

	result = x + y;
	printf("Addition of x and y is:%d\n", result);
	return (0);
}
